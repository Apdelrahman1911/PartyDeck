**Accepted for source and fixture review; Swift host compilation and native gameplay execution remain pending.** Independent review by `review_game`, 2026-09-10. This covers `AuthorityHost`, `AuthorityHostUITests`, authority staging, test orchestration and receipt verdicts. No gameplay implementation or Git state was changed, and no unchanged JVM suite or desktop match was rerun.

The actual generated `PartyDeckGodotBridge.h` from successful [run 34427976260](https://github.com/Apdelrahman1911/PartyDeck/actions/runs/34427976260) supplies the checked API. Its SHA-256 is `dddbb2ae23d1e87ebab18fa7c4f77326c3e370422b96f1e723c3e2f2cdde14fd`. `IosQualificationFactory.shared.create(presentationId:mode:randomness:reduceMotion:soundEnabled:textScale:)`, `.twoD`/`.threeD`, `.referenceSeed2`/`.secure`, the five throwing mutation methods, `status()`, `launchDocument`, result/status properties and inherited enum `name` all agree with the Swift source. The Objective-C class prefix is `PDGB`; the host correctly uses the header's unprefixed Swift names. Successful import-only compilation from the earlier run does not establish that this new caller compiles. The prior native framework and 20 Kotlin/Native shared tests are assessed separately in [ios-facade-review.md](ios-facade-review.md).

`AuthorityModel` constructs the real facade with a fresh UUID and passes its recipient-projected launch document directly to native preparation. Reference seed 2 requires an explicit launch argument; the default selects `.secure`. Renderer events reach `handleRendererEvent(document:)` unchanged. The owner concatenates the returned command strings and subsequent opponent-policy strings in their original order, without parsing, reconstructing or inventing views. Delivery bounds are 16 commands, 65,536 UTF-8 bytes per command and 262,144 bytes per batch. Rejection reconciliation explicitly requests `refreshView()`. Continuation remains a real renderer intent; Swift adds no game rule, strategy or outcome decision.

Facade calls execute synchronously on the main thread, with preconditions at native/UI entry points. The runtime's event-delivery depth guard keeps its drain from reentering the owner while callbacks are active. A weak callback captures the presentation generation; closing invalidates it, clears the native handler and drops the owned facade. A second start is refused while an authority exists or closing has begun. Foreground loss covers the native surface before pausing the facade, and resume updates the gate before renderer delivery. Resume also advances the existing opponent policy after Ready so initialization during foreground loss cannot strand an opponent turn. Terminal renderer outcomes, host close, preparation failures and native termination all dispose of ownership. Receipts contain coarse facade status and the native observation snapshot, not retained launch/event/view documents. Full native diagnostic validation and draw/cleanup behavior are the separate native/security review scope.

The five UI cases are substantive source assertions:

- Four seed-2 matches exercise 2D and 3D at text scales 1 and 2. Reveal, select, hide, native pause and actual application background/resume must preserve authority counters and clear private bindings. Native tap/drag coordinates come from read-only renderer geometry; they must fit inside the actual viewport and clipped control, agree with UIKit points, and provide a fully visible target of at least 44 points. Before tapping, the test rechecks the authority revision, native cover, target geometry and viewport. No test-only gameplay API or injected authority event supplies a move.
- Play, challenge and continuation require the corresponding authority-accepted counter to increase by one, other action counters to stay fixed, and the presentation revision to advance. A finished match must reach round 14/revision 41 with two viewer plays, two viewer challenges, thirteen continuations and a nonempty authority winner. Tests then use the real lobby control, including the 2D confirmation, and assert terminal ownership cleanup plus a responsive native shell.
- The default-secure case launches without the reference argument, waits for a real Ready/iterating renderer, taps its Exit control, and checks the accepted terminal outcome and unchanged viewer/continuation counters. It qualifies only secure construction, rendering and renderer exit when executed; it does not exercise a secure full match.

`prepare-authority-host.py` now requires the pinned engine/path configuration, exactly the two distinct linked archive receipts, their actual hashes/sizes and an exact current native-module source inventory. `build-probe.sh` emits the matching inventory. The stager also checks the ARM64 Simulator framework's binary/header/module-map/plist against its successful receipt, checks the staged copy, and invokes the existing verified-pack resource preparation. These guards prevent silently linking a stale native archive that lacks the new diagnostics interface.

`test-authority-host.sh` keeps build and test milestones separate, uses shell failure propagation, retains XCTest attachments/app output, and refuses to replace an existing result bundle. The recorder requires definitions of the Swift entry point, native runtime and both facade classes; undefined imports are insufficient. Its execution verdict requires exactly the five expected starts and five corresponding passes plus successful XCTest completion. Reference full matches and secure launch/exit have explicit, separate fields:

| Qualification gate | Status for these reviewed sources |
| --- | --- |
| Generated framework API and earlier native shared tests | Previously accepted in run 34427976260 |
| New Swift authority host compilation/link | Pending actual Xcode result |
| Four seeded full matches, 2D/3D at 100%/200% | Pending actual XCTest execution and attachments |
| Secure-default launch and renderer exit | Pending its separate XCTest case |
| Secure full match | Not covered; receipt remains false |
| Shipping KMP factory, dormant engine reentry, device Metal | Not qualified by this host; receipts remain false |

This reviewer independently executed **30 isolated Python fixture checks, zero failures**, against the final stager/recorder. They cover successful staging and build-only scope, correct test scope separation, missing/undefined owners, exporter-owned app contamination, missing/extra/duplicate/failed/skipped/unfinished tests, absent success markers, wrong framework targets/flags, changed or missing framework files, symlink staging, wrong engine pin/path mode, changed native archives, incomplete/duplicate/unexpected archive inventories, and missing/stale/extra native-module hashes. Binary files, XCTest logs and the downstream resource helper are explicit fixtures; these checks establish receipt rejection behavior, not native compilation or execution. Python parsing, shell syntax, Info.plist parsing and scheme XML checks also passed.

The reviewed snapshot is preserved at `/tmp/partydeck-ios-authority-host-review/source-final/`; `source-hashes.json` records all twelve source/configuration files. Principal SHA-256 values:

| File | SHA-256 |
| --- | --- |
| `AuthorityHost/AuthorityModel.swift` | `bcf2768777adcf97b50c5195b2acdc307559de0b259a13de1653276546876ac1` |
| `AuthorityHost/AuthorityHostApp.swift` | `f86d8371c199ffa543dbb81352a8b23459975de27ecfd5864d9e6bcc3ab5ef01` |
| `AuthorityHostUITests/AuthorityHostUITests.swift` | `2048fd3767286e3952faefb1be9c1ddc0bb3228c601c52bdfe29f9e06228b6fa` |
| `prepare-authority-host.py` | `e95fc6a1a46ed72fec5f548d947f86f3577c1a3a16cb20b61fb4eca6f998c39b` |
| `record-authority-result.py` | `b7bf81c44bdf96832910ed90f56f6c9c6e40a3bb214bbf1428a04c39983e01d2` |
| `test-authority-host.sh` | `50b57cc047df646ac57af628141b2cf3c97b2f8d0691162fc02e46dfc5093ac4` |
| `build-probe.sh` | `a90d8da3e5abf82b0aaa94d08fb35556c87473167af1448ea6fb006a1930538e` |

Reproducible fixture script and result: `/tmp/partydeck-ios-authority-host-review/receipt-fixture-review-final.py` and `receipt-fixture-review-final.json`. Apple documentation was retrieved directly on 2026-09-10 for [normalized element coordinates](https://developer.apple.com/documentation/xcuiautomation/xcuielement/coordinate(withnormalizedoffset:)), [coordinate taps](https://developer.apple.com/documentation/xcuiautomation/xcuicoordinate/tap()), [application activation](https://developer.apple.com/documentation/xcuiautomation/xcuiapplication/activate()), and [main-actor controller dismantling](https://developer.apple.com/documentation/swiftui/uiviewcontrollerrepresentable/dismantleuiviewcontroller(_:coordinator:)). Their JSON sources, declarations and hashes are retained under `/tmp/partydeck-ios-authority-host-review/authoritative/`. Kotlin error/list/enum export behavior uses the authoritative references already preserved with the facade review; native caller compilation remains the final check of Swift import behavior.
