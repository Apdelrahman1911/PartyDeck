package dev.partydeck.core

import kotlin.test.Test
import kotlin.test.assertEquals

class BootstrapTest {
    @Test fun kotlinMultiplatformTestRuntimeWorks() {
        assertEquals(15, (1..5).sum())
    }
}
