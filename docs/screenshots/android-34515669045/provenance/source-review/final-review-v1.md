Android job 103000331429 passed in combined Validate run 34515669045 at exact head 0f00f17a398be96aef7bdb58d03074a9914f8232. The overall workflow failed in the separate iOS native job 103000652450. This handoff covers the completed Android collection and review; it makes no iOS success claim.

Independent enumeration found 262 current JUnit tests across 41 suites and eight modules, with zero failures, errors or skips. All 41 suite timestamps fall inside this run's build step. The original Android log records 207 Python checker cases, all passing. Lint reported 11 warnings and zero errors or fatals. Historical documentation copies were excluded.

The emulator was verified as API36, 720 × 1600 at 280 dpi, font scale 1.0. Both actual APKs contained the qualification profile with 2D and 3D enabled. Ordinary UI smoke, native Ready, requested lifecycle checks and separate Standard-action checks passed. Both original native results explicitly request engine gameplay. Debug has 30 passed checks and 100 captures; the non-debuggable optimized APK has 28 passed checks, two explicit renderer-death skips and 86 captures. Both crash logs are empty, with zero diagnostic or environment-restore errors. The established per-APK 1,200-second and runtime-step 55-minute caps were unchanged.

The [collector XML audit](/root/projects/PartyDeck/artifacts/evidence-storage/34515669045/review/engine-action-original-xml-audit.json) and [independent game_domain review](/tmp/partydeck-android-engine-action-review-game-domain-34515669045-v1/original-action-review.json) support all four current engine scenarios. Each starts at a fresh human turn with five unchecked baseline cards, performs Reveal and slot-zero selection, then exactly one Play with no corrective swipe. The immediate Standard UI shows a concrete public result for the same baseline round. Sampled matching owner counts are:

| Variant | Mode | Matching owner originals | Immediate Standard outcome |
| --- | --- | ---: | --- |
| Debug | 2D | 49 | Same-round public result |
| Debug | 3D | 49 | Same-round public result |
| Optimized CI-signed | 2D | 49 | Same-round public result |
| Optimized CI-signed | 3D | 51 | Same-round public result |

All 198 owner observations match their scenario. Standard return and real Leave to Home preserve the shell/task and remove the renderer child; each isolated practice ends at Home. Local renderer selection clearing is observed. All current concealed-return, immediate outcome and later Standard XMLs contain zero game-card nodes, so the remaining Standard hand and unchecked remaining cards are unobserved. The older run 34506173394's 2D five-to-four hand observations do not transfer to this run. Both current 3D results include subsequent opponent progression.

The [package audit](/root/projects/PartyDeck/artifacts/evidence-storage/34515669045/review/package-audit.json) independently verifies debug, unsigned release, disposable CI-signed optimized APK, and release AAB bytes. All contain compact PCK SHA-256 557b2297bed133a433acc25efa4837462465dd4e06da659ae5a4cbd792f77fe0. All 550 unsigned application payload entries remain byte-identical in the optimized APK; only three signature entries are added. Distribution signing is false. The [native package and JNI binding](/root/projects/PartyDeck/artifacts/evidence-storage/34515669045/review/native-package-and-jni-binding.json) binds actual canonical packages to native inputs, CI-reported installed hashes, copied preflight inputs, and original activation manifests. Pulled installed-base.apk copies are absent from the original report ZIP, so those copies could not be independently rehashed. The complete optimized/unsigned DEX set exactly matches the frozen prior DEX, preserving the existing 122/122 selected core JNI declaration result without rerunning the parser.

Both original ZIPs and all 5,059 extracted Android files were independently verified against ZIP contents, CRCs, hashes, original manifests and API size/digest receipts in the completed [integrity audit](/root/projects/PartyDeck/artifacts/evidence-storage/34515669045/review/collection-integrity-audit.json). The [source binding](/root/projects/PartyDeck/artifacts/evidence-storage/34515669045/shared-source-reuse.json) reuses game_domain's frozen derived exact-head source: 841 retained files derived from parent 286b7ff11b2441daad044b57fad6fc8b6be995d0, with one changed iOS PCK literal. The prior preflight independently bound 21 selected Android files and raw Git blob identities. There is no original archive for this target head; the ancestor b080780 archive lineage remains explicit. Of the omitted gallery blobs, 1,124 have remote tree identity only, without independently collected or hashed payload bytes. The inherited gradlew.bat CRLF conversion remains documented.

The [capture inventory](/root/projects/PartyDeck/artifacts/evidence-storage/34515669045/review/capture-inventory-v1.json) indexes 318 PNGs: 186 native captures, 62 ordinary UI captures, one preparation capture and 69 Compose snapshots. This collector viewed no media and claims no direct-original view receipts. No videos occur in the original Android report manifest. Gallery and visual review belong to review_design. Optional unsigned-APK deduplication against the adaptive run was rejected at the manifest identity guard because the complete APK hashes differ; no hardlink or payload mutation occurred and zero bytes were reclaimed.

Play acceptance is an action-specific inference from the immediate same-round Standard public result. Renderer input counters, revisions and selection clearing are local observations, without an internal authority/session revision receipt. Ownership and freshness are sampled; there is no atomic input-delivery-before-expiry guarantee. Pixel privacy, TalkBack, adaptive layouts/fonts, physical devices, physical LAN, store signing and iOS are outside this Android qualification. Separate adaptive and later iOS runs retain their own results and cannot replace this run's originals.

Finalization hashes small receipts and control files and reuses completed payload audits. It performs no application/source edit, Git operation, CI dispatch, native execution, build, test or repeat of passing audits. The [integration handoff](/root/projects/PartyDeck/artifacts/evidence-storage/34515669045/integration-handoff-v1.json) and [freeze record](/root/projects/PartyDeck/artifacts/evidence-storage/34515669045/collection-frozen.json) bind the evidence and limits. Root owns integration, STATUS and the final qualification report.
