The first filtered UI invocation actually executed both cases and produced all four screenshots. A following filtered test removed its shared XML after it was read. The second invocation, under /tmp/partydeck-gradle.lock, restored that original XML from Gradle's build cache. Its timestamp matches the first execution exactly. The restored original XML was copied before lock release. No new rendering or duplicate screenshot is claimed for the cache recovery.
