The initial 2D Reveal clipping is a stable compact-layout issue. The isolated source probe reproduced the exact Android geometry at both four and sixty process-frame callbacks without changing scroll position. A small decorative-height reduction now keeps the whole concealed-hand panel visible at that geometry.

The only tracked change is `godot/renderer/presentations/two_d/table.gd`, frozen at SHA-256 `8d8650c8b6b6c5c5052b798e540d0b618dc2d916d7fdda0c95011eac6412f993`. In the existing narrow layout, when text scale is below 1.5 and the logical height is at least 500 but below 800, the decorative table minimum height is 124 instead of 148. Other height branches, text sizes, action minimum heights, scrolling, and input/privacy logic are unchanged. Root authorized application after its frame-reconciliation milestone and is responsible for export and native qualification.

The original normal-text Android captures from run 34439587132 contain the same Reveal geometry on match entry, scene re-entry, and native re-entry: `[32,512,339,56]`, clipped by `[16,118,379,446]`. The button extends four logical units, or seven physical pixels at density 1.75, below the scroll region. All three original PNGs have SHA-256 `52d8b06c0cb7a51e4b0dc0e87f31ed75bd4922491925ac792b6dcf57b218db3b`. The resumed capture places Reveal eighty logical units higher; the scene's reveal/cover scrolling explains that difference, so the resumed image was not treated as proof of startup settling. Exact copies of these four native originals and paired JSON are retained under `native-originals/`.

The graphical probe loads the real frozen scene with a recipient-safe launch generated by the actual seeded qualification authority. It uses Godot 4.7.2, OpenGL Compatibility, Mesa llvmpipe, and Xvfb. The normal-text surface is exactly 720×1244 at scale 1.75, yielding the same 411.428558×710.857117 logical viewport as Android. Captures use an actual fresh renderer frame and do not move scroll.

| Case | Reveal rectangle | Cover rectangle | Clip bottom | Scroll | Result |
| --- | --- | --- | --- | --- | --- |
| Frozen source, 100%, early and late | `[32,512,339,56]` | `[16,432,371,148]` | 564 | 0 | Reveal overflows by 4; cover by 16 |
| Adjusted source, 100%, early and late | `[32,488,339,56]` | `[16,408,371,148]` | 564 | 0 | Reveal and full cover fit; cover has 8 units below it |
| Both sources, 200%, early and late | `[32,476,339,72]` | `[16,339,371,221]` | 676 | 0 | Identical geometry and PNG bytes |

Normal-text baseline observations were taken 150 and 1123 ms after launch; candidate observations were taken at 161 and 1138 ms. Within each case, early and late PNG bytes are identical. All four 200% baseline/candidate PNGs have SHA-256 `b1ae240dd4575fc003a9b14c5b8f5b232c767cbbe9facb5a2a6e2c7930721e0b`. The large-text surface is 720×1204 at the same scale. The original 151-file renderer snapshot is unchanged, and the isolated candidate differs in only the intended table script.

The existing repository-owned 2D scene checker also passed against the tracked source at 411×711 logical units with touch emulation and lobby checking enabled. It exercised a drag starting on Reveal without activation, small-motion card taps, deselection, horizontal hand dragging without selection, first/last-card selection, cover and foreground erasure, resumed concealment, lobby cancellation/confirmation, pending-input rejection, and Close. Five original PNG/JSON pairs and the passing report are retained under `tracked-compact-scene-native-host-font-1.0/`. The runner requires the passing report, all expected captures, exit code zero, and no engine/script errors; all passed.

One earlier scene run is explicitly retained as failed under `tracked-compact-scene-font-1.0/`. It completed touch and privacy assertions, then stopped at the optional lobby precondition because the comparison fixture grants return only after the match finishes. Four captured originals, the failure document, and logs remain unchanged. The corrected fixture uses exactly the same authority-projected GameView, with early-lobby native permission supplied through the real bridge adapter and encoder. Neither the renderer nor the checker was changed for that correction. This is a static permission fixture and does not claim that a gameplay or lobby intent was accepted by the authority.

There are seventeen newly captured original PNGs across the four probe cases, failed scene run, and passing scene run. Their separate paths, hashes, and dimensions are inventoried in `provenance.json`; identical and failed originals are retained individually. The design reviewer has received all paths for independent review.

These checks establish the layout correction and local source input/privacy behavior on the desktop renderer. They do not qualify Android EGL restoration, device touch delivery, accessibility, or a newly exported native package. The original Android observations remain associated with their original run and source. Root must export the frozen source and retain the new native qualification separately.
