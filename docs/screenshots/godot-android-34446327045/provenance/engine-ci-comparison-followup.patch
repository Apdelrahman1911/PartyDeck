--- a/docs/research/engine-ci.md
+++ b/docs/research/engine-ci.md
@@ -95,6 +95,14 @@
 
 Both **Android 3D cases still fail before a gameplay intent**. Normal text reaches Ready but times out native diagnostic request 2 while retaining response 1; its final host observation is active with a ready foreground frame and the timeout latched. At 200% text, reveal/select/hide and the Home/recents captures execute, then the actual engine process receives SIGSEGV during the background sequence. Its log records the emulator GLES encoder's `s_glVertexAttribPointer` GL error immediately before fatal signal 11. This is an actual native crash, distinct from the precisely paired shader-cache warning that the checker now accepts. The overall run therefore fails the four-case gate. The successful 2D scope does not establish optimized Godot runtime, API 36, physical-device GPU/audio/accessibility, LAN, same-process reinitialization or store qualification.
 
+The fifth [Godot comparison run 34446327045](https://github.com/Apdelrahman1911/PartyDeck/actions/runs/34446327045), at commit `b7f2f4f2c2d03b8e599d4b0fc73bf2d35f6640eb`, passes 29 actual JUnit cases, 69 checker host tests, 111 packed native-boundary assertions and both desktop renderers with identical 42-view authority traces. All 22 desktop PNG hashes and provenance match. Its exact-source PCK check accepts 136 entries, SHA-256 `0ed324c19016f28987d6294f915c107f62234670b149f73bab585af93e29ae6d`; both APKs and the AAB embed that pack. The debug APK, unsigned release APK and AAB archive sizes are **314,313,858**, **311,447,245** and **104,824,251 bytes**, respectively. All four native cases install the same verified debug APK, SHA-256 `8f6cbccbb1313ff2f0d17da5879275ebfc0f25b5d8592ce884d02bc3f91d9a76`, after fresh API 35 preparation without a reboot. The workflow still fails because both 3D cases fail their final native Back route.
+
+**All four native matches now complete the scripted gameplay and match return.** Independent host traces confirm two viewer plays, two challenges, thirteen round advances, the round-14/revision-41 winner and one accepted Return to lobby. All retain their original process/presentation through background and resume, observe the native cover, and return with private bindings and selection cleared. Each then opens a distinct fresh presentation for scene Exit and another for the native close/back check. All 48 scene capture crop hashes match, all 156 scene input records match retained renderer geometry, and all 78 original PNGs decode. Direct review of 20 originals includes all four opaque recents previews, concealed resumed scenes, winners and post-match choosers, plus both 3D final failure screens and fresh Back-test entries. Both 200% winner frames require scrolling to reach the return action; the subsequent recorded input and teardown establish that return. These selected stills do not establish continuous pixel privacy or general design/accessibility approval.
+
+**Both 2D cases pass their complete three-entry lifecycle; both 3D cases fail late at native Back.** The four match exits, four scene Exits and two 2D native Closes have complete teardown receipts, enabled surviving-chooser controls and actual old-process absence checks. The two 3D Back entries instead report `closeReason=renderer_exit` where the checker requires `native_back`, and increment received events from one to two where a native-local transition requires no event change. Both retain all close/destruction markers, zero gameplay intents/rejections, no diagnostic timeout, and two failure snapshots with an empty engine PID list. The expected-route check fails before the dedicated teardown success loop, so neither has a successful `teardown.json`; neither is relabeled as passed.
+
+The retained Android event logs record the actual Back presses at **06:57:35.865 UTC** (normal text) and **07:01:24.254 UTC** (200%). Native destruction takes **101 ms** and **134 ms**; process logs record engine termination, render-thread exit and process-exit requests. Android records child PIDs **9147** and **10850** dying at **06:57:36.294 UTC** and **07:01:24.779 UTC**, respectively. The same chooser PIDs **2849** and **2780** resume, and both original final screens show the chooser. This supports actual shutdown after the wrong close route, while preserving the route failures. The preceding startup diagnostic timeout and emulator GL abort do not recur in these retained entries; that observation alone does not prove a general fix. All twelve process logs still contain an `eglCodecCommon removeVertexArrayObject` error during shutdown, and ten contain the paired shader-cache warning at error priority. No clean-driver-log, optimized Godot runtime, API 36, physical-device GPU/audio/accessibility, LAN, same-process reinitialization or store qualification is claimed.
+
 ## Required validation jobs
 
 ### Android and shared behavior
