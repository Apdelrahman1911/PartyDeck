Approved from source review: the exact frozen Card3D/table reuse candidate preserves the established resource ownership, privacy, input and teardown boundaries. No source blocker was found. This approval covers the two-file code delta only; it does not attest parsing, execution, rendering equivalence, performance improvement, native integration or PCK qualification.

The reviewer independently copied and compared all 155 renderer source files against the frozen `source-c65e264` inventory: 153 are unchanged, and only `presentations/three_d/card_3d.gd` and `presentations/three_d/table.gd` differ. The independently derived unified diff matches the owner's patch byte-for-byte. The [identity verification](/tmp/partydeck-3d-resource-reuse-security-candidate-v1/identity-verification.json) records every file and no mismatches. Candidate, baseline, freeze and patch copies are retained in this reviewer workspace.

| Item | SHA-256 |
| --- | --- |
| Owner source freeze | `8f1aa984995a77956c651034233ea45aaeb99f1320b068c428616d3e4199338a` |
| Source inventory binding | `b2498436798a2e44ff184ec380ce22c7127576531fe822ab77452293e42f3d4a` |
| Candidate patch | `a904d39cb259cae75085001f4733a0d7ee49cd1f2693f8c723ae914541c9b484` |
| Candidate Card3D | `6383be0e044f1f54792ab3f575917ef29a2d843e697f792d748627059c9bcba5` |
| Candidate table | `a9edd383b71832b41d3fd307d3cf4397fe33df099c54471cb16006da0246e061` |
| Preserved `godot/reviews/security-review.md` | `bee360e6feee86077df66fb5ad762d791067bb902cdd307832c892b7d44fde15` |

The owner's workspace is `/tmp/partydeck-card3d-reuse-candidate-assets-v1-n6xs0bjq`. The reviewer made no owner, product or shared-file edits. Review operations were source/metadata reads, static byte/hash comparisons and copies/notes under `/tmp`; no product/checker imports, tests, probes, builds, exports, Git commands or engine/native/device execution occurred.

[SharedResources](/tmp/partydeck-3d-resource-reuse-security-candidate-v1/candidate/godot/renderer/presentations/three_d/card_3d.gd:6) is a RefCounted bundle with exactly three mesh references and two solid-material references. It holds no nodes, callbacks, controllers, card IDs, textures or private game state. The static factory creates a new bundle on each call; there is no static resource variable or global cache. Mesh dimensions, material colors and roughness exactly match the baseline. All writes to those properties occur inside that factory before it returns; no candidate consumer mutates them later.

[The table factory](/tmp/partydeck-3d-resource-reuse-security-candidate-v1/candidate/godot/renderer/presentations/three_d/table.gd:498) lazily creates a bundle in the table instance, constructs a fresh Card3D and injects the bundle before returning it. Both original attachment paths now call this factory, so claim, hand and revealed cards receive initialized resources before `add_child()` can run `_ready()`. A project-wide source search found no other Card3D construction path. The assertions document the precondition; the actual call order satisfies it without depending on assertion execution.

[Card setup](/tmp/partydeck-3d-resource-reuse-security-candidate-v1/candidate/godot/renderer/presentations/three_d/card_3d.gd:42) still constructs fresh MeshInstance3D nodes. It shares their geometry and the two invariant solid materials only. `configure()`, `lift()` and `corners()` are unchanged. Each configuration still creates a unique face material with the original texture selection, filtering, shading and alpha-scissor properties. Selection visibility and transforms remain on individual nodes, and tweens remain per card. The add-only private-face group therefore remains safe under the existing fresh-node lifecycle.

The table's private labels, selected markers, accessibility names, metadata, button state, captured card-ID callbacks and `_card_bindings` logic are unchanged. `_show_cards()` continues to clear cards, ornaments and hits before replacement; `_clear()` still detaches children before deferred deletion. The current callback tree/deletion guards and table/controller revision checks remain intact. No old node or callback is pooled into a new state.

[Teardown](/tmp/partydeck-3d-resource-reuse-security-candidate-v1/candidate/godot/renderer/presentations/three_d/table.gd:957) adds only `_card_resources = null`. The table releases its bundle reference when leaving the tree. Existing Card3D nodes retain their bundle references, and MeshInstance3D nodes retain the mesh/material references they need, until their normal destruction. The bundle has no back-references and contains only public invariant render properties, so this does not extend private face, label or input ownership. Existing callback disconnection, feedback stop and state/controller/revision/focus/scroll clearing remain unchanged. Each new table starts with an empty instance field and cannot obtain the previous table's bundle through this code.

These conclusions apply the already verified Godot `4.7.2-stable` semantics at commit `ed1daf0bf001b61586d9930840f2f1394092c079`, without repeating that source audit: [mesh assignment](/tmp/partydeck-3d-reuse-security-review-v1/engine-source/scene/3d/mesh_instance_3d.cpp:120) retains the supplied mesh reference and obtains its RID; [material assignment](/tmp/partydeck-3d-reuse-security-review-v1/engine-source/scene/3d/visual_instance_3d.cpp:218) retains the supplied material reference; [PrimitiveMesh updates](/tmp/partydeck-3d-reuse-security-review-v1/engine-source/scene/resources/3d/primitive_meshes.cpp:51) generate shared geometry; and [node removal](/tmp/partydeck-3d-reuse-security-review-v1/engine-source/scene/main/node.cpp:1747) synchronously detaches the subtree before deferred deletion. The candidate configures shared resources before assignment and leaves them immutable, matching those requirements. The full semantics and their exact bindings are in the [prior research report](/tmp/partydeck-3d-reuse-security-review-v1/SOURCE-RESEARCH.md).

Viewport dirty/signature handling, stage/layout behavior, MSAA_2X, public ornaments, asset loading and all other renderer source files remain unchanged. The known reduction is repeated construction of three meshes and two solid materials per card, with a lazily initialized bundle retained by each active table. Shader compilation, texture I/O and measured latency are not inferred from that source count. Owner validation and evidence qualification remain required before integration under the root's plan.
