No blocking rendering or geometry finding was found in the exact Card3D resource-reuse patch a904d39cb259cae75085001f4733a0d7ee49cd1f2693f8c723ae914541c9b484. The candidate preserves the reviewed rendering behavior and passes the existing local desktop checks. This source approval supports root's integration and subsequent platform validation; it does not establish an iOS startup-time improvement or change the failed retained native qualification.

The reviewed owner root is /tmp/partydeck-card3d-reuse-candidate-assets-v1-n6xs0bjq, full freeze SHA-256 7c274914ff2f3d8e500458ef5303492e02d2af2144c04e2d16f3659bf8a8b5e3. All 155 baseline and candidate source files were independently checked. The baseline equals both frozen c65e264 and diagnostic fa0fabb renderer bytes. Exactly card_3d.gd and table.gd differ; the other 153 files match. Reconstructing the unified diff from the original and candidate files reproduces the pinned patch byte for byte. The candidate file hashes are 6383be0e044f1f54792ab3f575917ef29a2d843e697f792d748627059c9bcba5 and a9edd383b71832b41d3fd307d3cf4397fe33df099c54471cb16006da0246e061, respectively.

The table now lazily constructs a typed RefCounted bundle with three meshes and two solid materials. Every field is initialized before the bundle reaches a card. Both existing card construction paths call _create_card(), which injects the bundle before add_child() can invoke _ready(). The complete source/scene/resource reference search finds no alternative Card3D constructor or serialized card scene. Card3D nodes and their three MeshInstance3D children remain distinct per card. Source writes to the shared mesh dimensions and material configuration occur only in the factory. The factory is a static function, but the bundle storage is per table instance.

| Resource or behavior | Preserved configuration |
| --- | --- |
| Border BoxMesh | Size (1.11, 0.018, 1.65); node y=-0.023; initially hidden; selection toggles only that node's visibility |
| Body BoxMesh | Size (1.035, 0.035, 1.575) |
| Face PlaneMesh | Size (1.06, 1.59); node y=0.02; same default plane orientation/subdivision |
| Border solid material | CITRON #d6ef82; roughness 0.9 |
| Body solid material | PAPER #f4f0e8 darkened by 0.09; roughness 0.9 |
| Face material | Still created per card by the unchanged configure(); same texture selection, unshaded mode, alpha scissor, and filtering |
| Geometry interaction | corners() and lift() are byte-identical; table transforms, labels, targets, and callback construction are unchanged |

The exact method comparison preserves configure(), lift(), and corners() in full, along with 49 table methods including redraw preparation, frame signatures, render/update gates, state application, input, layout, and cleanup helpers. In _show_cards() and _add_card(), only Card3D.new() becomes _create_card(). In _exit_tree(), only the table's bundle reference is cleared. No project settings, anti-aliasing, lighting, imports, fonts, texture files, viewport modes, or drawing suppression changes occur.

Ten Godot implementation files were independently decoded from pinned commit ed1daf0bf001b61586d9930840f2f1394092c079, with source-object hashes and exact checkout equality recorded in engine-source-binding.json. MeshInstance3D::set_mesh at scene/3d/mesh_instance_3d.cpp:120 retains the Ref<Mesh> and uses its RID; it does not clone a mesh per node. GeometryInstance3D::set_material_override at scene/3d/visual_instance_3d.cpp:218 stores the material on the individual geometry instance, and MeshInstance3D::get_active_material at line 393 gives that override precedence. Therefore sharing the face plane does not share the per-card face material. PrimitiveMesh::get_rid at scene/resources/3d/primitive_meshes.cpp:234 materializes pending geometry; its size setters and fixed default orientation/subdivisions preserve the moved dimensions. RefCounted references keep resources valid until their holders release them. Node ready/exit ordering and the observed ownership probe support injection before ready and safe release during teardown.

The table keeps the bundle across rebuilds, including any temporary zero-card state, and releases its table reference on exit. Card references keep the bundle valid while those nodes still exist. There is no static/global bundle storage or back-reference from the bundle to a card/table. The existing owner-run contract probe observes all five resource references and the bundle released after Close, then observes fresh resource identities on the next presentation.

For the first nonempty K-card batch, the source avoids 3(K-1) mesh constructions and 2(K-1) solid-material constructions. Later batches reuse those same resources, avoiding 3K mesh and 2K solid-material constructions. A single RefCounted bundle is added per table lifetime. Face materials, nodes, ornament resources, and other work remain as before. This is a resource-construction reduction, not a measured duration reduction. BaseMaterial3D::_update_shader at scene/resources/material.cpp:656 already reuses shader keys across materials, so resource counts cannot be converted into shader compilation counts. The native diagnostic's actual card counts were not bound to these local fixtures.

All original local validation receipts, logs, executed checker source files, and reports were independently rehashed against the owner's full freeze. Their exact source hashes match the reviewed files. Thirteen recorded runs include two imports, four scene cases for each of baseline and candidate, and three candidate behavior checks. The numbered checks comprise 306 redraw checks, 216 terminal-return/lifetime checks, and 38 resource-sharing/lifetime checks: all 560 pass, with exit code 0 and no script/engine errors. Scene-check procedural assertions are separate from that count. The rendering runs use real Godot 4.7.2 OpenGL Compatibility under Xvfb with Mesa llvmpipe; only imports use headless mode. The normal unsupported V-Sync warning is present on this software driver.

The sharing probe remains outside the candidate project. Its source-test projection explicitly adds three public claim backs to the existing own-hand fixture. It observes three shared mesh identities and two shared solid materials; unique face materials and three mesh-instance nodes per card; fresh card nodes across reveal and resize; persistent resource identities across reveal, selection, and resize; and released/fresh resources across Close and reentry. Its projection is not an authority-accepted play or a description of native run 34525504734.

All 20 original baseline/candidate PNG pairs were independently rehashed and compared as exact bytes, with dimensions checked from their PNG headers. All match, covering portrait, landscape, enlarged text/drag, and resolved public proof states. No images were decoded, viewed, extracted, or copied by this reviewer. Exact equality supports equivalence on that local software-rendering stack; it does not establish native antialiasing, driver behavior, native accessibility, or native cover/presentation timing.

The security review's ownership/privacy conclusion remains separately owned by review_security. This review confirms the rendering source and audits original desktop execution; it introduces no shared edits, Git operations, engine/native execution, dispatch, export, or canonical artifact. The independent phase interpretation for diagnostic run 34525504734 remains frozen separately with two native passes, one failure, and both current second-entry observations rejected. Its callback intervals do not assign exclusive mesh/material, shader/compiler, GPU, or CPU rendering causality. Root owns integration and the remaining native qualification.
