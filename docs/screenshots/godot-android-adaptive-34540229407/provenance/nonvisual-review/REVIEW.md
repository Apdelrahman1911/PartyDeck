# Android adaptive run 34540229407 — independent review

All four original consumers record **100 of 100 named scopes reached and passed**: 96 adaptive scopes and four installations. There are zero failed, unsupported, or unreached scopes and no main or restoration errors. All five CI jobs completed successfully.

| Consumer | Passed / planned | Held proofs | Rejected XML | Recorder receipts |
| --- | ---: | ---: | ---: | ---: |
| debug-font-1.0 | 25 / 25 | 4 | 2 | 8 |
| debug-font-2.0 | 25 / 25 | 4 | 3 | 8 |
| optimized-test-signed-font-1.0 | 25 / 25 | 4 | 4 | 8 |
| optimized-test-signed-font-2.0 | 25 / 25 | 4 | 4 | 8 |

The exact pinned predicates replay successfully for all 16 accepted returns, using the original control XML, display geometry, shell/task/native state, live target input and device-clock logs. The conservative remaining time before scheduled matching UP is **23.227–25.818 seconds**. All 77 selected state JSON records are reproduced from their 231 original Activity, window and process logs. The review independently rehashed 650 used selected originals and bound them to the sole collector’s extraction and member inventories.

All 13 rejected samples correctly fail because their own earlier state retains a native ActivityRecord: nine `DESTROYED`, four `STOPPING`. Every rejected XML has the same hash as its later accepted XML and includes a genuine enabled Reveal marker. Original state association is decisive. Their host receive times precede completed input transport by 25.395–26.117 seconds; that is not an independent device-UP proof, and rejected XML is never acceptance evidence.

The 32 original recorder and ffprobe JSON receipts are consistent with joined startup observers, positive growth within the original eight-second limit, matching process identity, and successful owned stop. Four initial reads found no output file yet; subsequent successful reads establish growth. This reviewer performed no media decoding or image viewing. Recordings remain `captured-review-required`.

Source head: `4ee4563def1c9a1b5060572efa2c3959c7d7677e`. Checker SHA-256: `9ed771c96e7f16a8c669e4db5c11904836c0e3937341ad3513a76f037401b918`. The current standalone PCK was independently hashed as `541ded4c5074b56a9e320882a23f357ff8162520fac099e32567ac3a678f2751` (1,550,440 bytes). Current input and installed APK identities match the collector’s retained members and all four independent package-admission receipts. Final producer SDK/JNI/build qualification remains a separate handoff; no earlier run’s qualification is transferred.

The result is limited to the recorded UI/lifecycle scope. Enabled Reveal establishes the current Standard privacy permission; it does not establish connection readiness, no pending action, cleared selection, COMPOSE-versus-CLOSING state, pixel privacy or cover order. Playable native gameplay, Back/card dragging, physical devices/LAN, TalkBack, iOS, distribution signing, full-hand identity and internal session identity are not inferred.

The complete named-scope enumeration and proof references are in `review.json`. Per-consumer `outcome.json`, active `evidence-review*.json`, `raw-state-review.json` and `collection-binding.json` retain the detailed checks and pins. The original earlier optimized 1.0 evidence review is superseded by `evidence-review-v2.json`; both are retained. All active reports use the corrected recorder startup audit.
