**2D initial Reveal allocation proposal — run 34488350932**

The bounded patch makes the whole Reveal control initially visible in the tested normal-text phone viewports. It changes only `godot/renderer/presentations/two_d/table.gd`. The candidate is outside the shared checkout; no canonical export, PCK, Git mutation or native CI dispatch was performed.

Baseline source is archived commit `4f694296b08899176831e7c444c6f8082d40d64b`, from the run using PCK `d6adbba1b5cbd6a1ac3a5754e4294363eae70ae9540af4ba12040cd4cfff44e0`. The current and archived table scripts matched SHA-256 `de659a18de07c3f606f7926059d90e0fe135a9ccb419a13a5e2dc7ab1c1f3fbf` before the proposal. `baseline-provenance.json` records the original measurement files, source files and executable. Original screenshots and measurements retain their case/name/time associations; this work does not assert a unique same-frame screenshot/measurement pairing. Retained measurements carry their PCK hash directly; Authority identity remains bound through the selected run/source-map evidence.

The defect is a fixed allocation, not a disabled control or missing private state. Godot's vertical automatic ScrollContainer gives the page a small minimum height while its content keeps the combined minimum height of the public table, cover and seats. Fixed header/turn/actions leave a 391-unit body in the retained host and a 427-unit body in Authority. The previous 124-unit decorative surface, paragraph and hand eyebrow place Reveal at y=514 with height 56 in both views. Pinned Godot 4.7.2 reproduces those native rectangles exactly from the archived source. The checked upstream container behavior and commit-linked source references are in `pinned-layout-sources.json`.

| Initial case | Baseline Reveal bottom | Candidate Reveal bottom | Body clip bottom | Candidate clearance |
| --- | ---: | ---: | ---: | ---: |
| 378×655 normal, retained viewport | 570 | 357 | 509 | 152 |
| 378×691 normal, Authority viewport | 570 | 357 | 545 | 188 |
| 320×568 normal | 602 | 357 | 422 | 65 |
| 320×740 normal | 602 | 357 | 594 | 237 |
| 320×568 normal, duplicate 24-character names with opponent turn and latest claim | — | 415 | 422 | 7 |

For the existing narrow layout below 800 logical units high and above the short-landscape breakpoint, the candidate omits the decorative public table surface, back illustration and redundant YOUR HAND eyebrow. It uses the already translated “%d cards, covered.” title without the explanatory paragraph. Turn, required rank and the complete latest-claim text remain. Control parents, ordering, callbacks, 56-unit minimum targets, scroll handling, authority and privacy logic are unchanged. The 200% path, tall portrait, short landscape, 900-wide and desktop comparison geometry remain unchanged. The separate 3D source and shared controller/main scene are untouched.

`two-d-initial-reveal.patch` is the integration proposal. `candidate/presentations/two_d/table.gd` has SHA-256 `de0bec7b083327f1210a9413d0236208be832ed49c0cec329b4999fb2994ba9a`. The normal-text compact range is `width < 860`, `500 <= height < 800`, `textScale < 1.5`; this reuses the source's existing narrow, short-window, compact-height and large-text breakpoints. No automatic initial scrolling was added. Hidden ornamental minimum sizes no longer determine where Reveal appears.

Targeted validation is summarized by `results/validation-summary.json` and can be rechecked with `python3 scripts/verify_results.py` from this directory. It checks 13 initial candidate cases against 13 archived-source cases, plus nine valid wrapping cases. All nine normal-text sizes have full Reveal containment at scroll zero; turn/rank/claim also fit in the wrapping cases. The visible action geometry is stable from observed frame 2 through frame 12. Concealed cases contain zero private faces, labels or selection. Four 200% cases retain identical control and scroll measurements. This establishes the tested dimensions/content; it does not claim every possible glyph/name combination.

Three existing source checks passed using real Godot OpenGL Compatibility rendering under Mesa/Xvfb: 378×655 normal, 320×568 normal, and 320×568 at 200%. Each executed reveal, first/last selection, cover, foreground loss/resume and close, plus emulated page/hand dragging and a small-motion card tap. Page drags did not reveal the hand; hand drags did not select cards; small taps selected correctly; cover/resume erased private faces, labels and selection. Local hand operations emitted only the expected Ready event. These checks do not accept authoritative play or challenge. Their commands, input hashes, raw logs, reports and exit codes are in each `results/source-input-*/execution.json` directory.

At 320×568 and 200%, the unchanged initial Reveal remains partly below the body clip. The source checker scrolls it into view, then completes the input/privacy check. Its initial scroll-to-target is 93 units and the emulated page drag advances it to 377. This is preserved scrolling behavior, not a claim that all enlarged-text controls fit at once. Horizontal partial neighboring cards and lower seat content likewise remain scrollable. Native input, native accessibility, actual device performance and a candidate PCK still require the coordinator's subsequent qualification.

All 12 new original PNGs are retained under `results/source-input-*`, with dimensions, SHA-256 values and paired diagnostics in `new-pngs.json`. `view-receipt.json` records the six actually viewed candidate images and three previously viewed native originals; all captures/views were reported to `review_design`. No derivatives were created. The new fixtures belong to the gallery batch following the completed 342-original batch. Independent design acceptance of this candidate is pending.

The earlier source-only candidate and measurement harness versions are preserved under `history`. The first wrapping-fixture attempt was rejected because its unavailable Play action retained a nonzero playable count; the harness was corrected to emit a valid recipient-safe fixture, and the failed log is retained. It produced no PNG. Final source checks all use the candidate hash above. Superseded source-only measurement results remain available and are not presented as the final candidate.
