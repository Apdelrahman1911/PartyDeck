# Debug font 2.0 — 3D seascape rotation pixel receipt

Reviewer: `/root/pixel_d2_sea`. Run `34540229407`, attempt 1, source `4ee4563def1c9a1b5060572efa2c3959c7d7677e`.

87 retained frame identities: 40 direct full-resolution inspections, 47 verified exact-byte matches, 0 unviewed. Original MP4 and all 93 decoder-frozen files match their recorded sizes and SHA-256 values. Every repeated WebP was compared byte for byte to its direct representative.

No private card faces were observed. Native landscape shows readable Show hand but clips lower instructions and leaves the lower table/hand area outside visible content (indices 39–57). Returned Standard portrait shows the full concealment panel; a lower rule/help line is clipped (indices 69–86).

These observations establish recorded pixels only. They do not establish offscreen concealed state, scrolling/reachability, continuous privacy between frames, input-event/UTC alignment, device/network qualification, or a preference between the two real Godot presentations. Historical reviewers retain their attribution; earlier receipts were not changed or re-viewed.

`receipt.json` contains every source identity, original packet PTS/time base, hash, direct or byte-match coverage, direct representative and reviewer, pixel notes, findings and limitations. `observations/` contains notes persisted after each displayed batch. `FROZEN.json` seals all receipt files.
