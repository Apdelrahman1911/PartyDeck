# Frozen iOS still handoffs — run 34554200601, attempt 1

Exact head: `253e9656a360a4411cab858484e1bd3dc2093a46`. Native job: `103123585125`.

- `failed-2d-stills-v1.json`: all six original PNGs from the failed 2D case, for `/root/pixel_d1_land`.
- `passed-3d-stills-and-scroll-v1.json`: all 21 original PNGs from the passed 3D case and the original body-scroll JSON pointer/accepted coverage, for `/root/pixel_o1_land3`.
- `publication-27-stills-v1.json`: all 27 original PNGs, chronological, for `/root/gallery_frames`.
- `provenance-receipts-v1.json`: bounded source/outcome/log/freeze references and ten exact metadata receipt identities recommended for retention, including the independent final preservation PASS receipt.

The original outcome is one pass, one failure, zero skips, XCTest exit 65. The combined two-case gate failed. All six failed-2D stills retain that case outcome. All 21 3D stills retain their passed case outcome. Exported test labels and failure-association flags are preserved verbatim; they are not independent visual judgments.

The three pairs of repeated 3D recorded hashes remain separate original capture entries. Original zero-based capture-pointer indices, timestamps, filenames, bytes, hashes, archive members and export metadata are retained. The sole video is outside this selection; no video asset or pointer is included. The 3D body-scroll support records one gesture with zero authority intents before/after; Challenge was unavailable and not exercised.

This supplement was compiled from frozen metadata. No image/video/opaque UI payload was opened, copied, decoded or rehashed. No original collector, preservation audit or native test was repeated. No original frozen file or donor hardlink was modified. Pixel review belongs to the assigned reviewers. Preservation PASS does not turn the combined native gate into a pass and adds no device, signing, physical-network, shipping or hardware-performance acceptance.

`FROZEN.json` pins every other file in this separate read-only metadata directory. Original source receipts and logs are referenced, not copied. Root owns resource registration and publication integration.
