# Game engine boundary and CI research

Verified on 2026-09-09 against upstream documentation, source, release metadata, and GitHub runner manifests. This document distinguishes the initial implementation from the additional work required to ship an embedded engine game.

The newest user instruction starts real **2D and 3D Godot qualification in parallel**, superseding the earlier engine deferral. Compose remains the production baseline while the real Godot scenes, platform hosts, event boundary and lifecycle are qualified independently.

## Initial implementation decision

The initial implementation ships the first game with Compose inside the KMP application shell and registers only implemented, playable games in the common catalog. Engine presentation remains optional through a common event interface and a platform-provided factory. The initial Compose baseline does not start or simulate Godot; the newly requested qualification work is separate.

Android has an official Godot embedding path. Equivalent iOS embedding into an existing application is a separate integration project with unresolved upstream surface/lifecycle work. Claiming Android/iOS Godot parity now would exceed the evidence. A future engine game must pass the gate below before it becomes selectable.

## What the Godot sources establish

| Area | Verified evidence | Consequence for PartyDeck |
| --- | --- | --- |
| Stable engine release | The official repository reports **4.7.2-stable**, released 2026-08-18. | This is research context, not a dependency selected for the Compose release. Recheck compatible engine/export-template versions when starting an engine integration. |
| Android embedding | The official Android library guide documents an AAR published as `org.godotengine:godot`, a host-owned lifecycle, `GodotHost`, `GodotFragment`, and bidirectional communication through a runtime `GodotPlugin`. | An Android factory can eventually create a game-only host and translate coarse events through plugin signals/methods. The shell remains the application owner. |
| Android constraints | The guide warns: “Currently only a single instance of the Godot Engine is supported per process.” It also warns that automatic resizing/orientation configuration events are unsupported and may crash. | Do not instantiate one engine per card/game tile. Prove repeated entry/exit, process recreation, backgrounding, insets, and supported size/orientation changes on actual target devices. Do not assume an orientation lock resolves every Android window configuration. |
| Android assets | The guide supports packaged project assets or a PCK/ZIP selected with `--main-pack`, and describes asset filtering needed for Godot's hidden directories. | A future integration needs a real asset export/package pipeline and matching engine resources. An empty view or dummy scene is not shipping support. |
| iOS export/plugins | Official iOS documentation covers exporting a Godot-owned Xcode application and extending it with static-library/XCFramework plugins. It does not document an Android-equivalent drop-in host view. | Exporting a Godot project or writing an iOS Godot plugin does not by itself prove the KMP-owned application can embed and dispose of the engine. |
| Library C API | The 4.7.2 source has `libgodot_create_godot_instance` and `libgodot_destroy_godot_instance`, marked `@since 4.6`. The header does not provide the native rendering-surface injection proposed upstream. | A library entry point is useful evidence, but is not proof of mobile host-window integration or lifecycle correctness. |
| iOS/native-view work | Official proposal [#1473](https://github.com/godotengine/godot-proposals/issues/1473), for an embeddable iOS view, remains open. Proposal [#14435](https://github.com/godotengine/godot-proposals/issues/14435), opened around native-surface injection for LibGodot, also remains open. The latter describes fragile platform workarounds; these are a contributor's reported experience, not a supported API guarantee. | Do not implement proposed functions as if they already exist. Budget an iOS native integration spike, source maintenance, and device validation before selecting a renderer. |

The Android integration cost includes an engine AAR, exported assets, a platform host, lifecycle/thread dispatch, GPU memory, and additional testing. iOS adds native source/library packaging and proof that the existing SwiftUI/KMP host can own presentation and teardown. No application startup-time, installed binary-size, frame-rate, or engineering-duration measurements have been made, so no numeric production cost estimate is claimed.

The isolated native engine compile passed in [run 34415851126](https://github.com/Apdelrahman1911/PartyDeck/actions/runs/34415851126) at PartyDeck commit `02364e5f3a4dd7742b43f8595269ff610acd2e4e`, using unpatched Godot `ed1daf0bf001b61586d9930840f2f1394092c079`, Xcode 26.4.1 (17E202), Apple clang 21.0.0 and SCons 4.11.1. The ARM64 simulator template build enables the Compatibility renderer and includes the custom native surface probe. The downloaded static archive is **191,336,704 bytes**, SHA-256 `aa97ac8ab7be8f1d0ea9947ee58bfc791e31ae64640b969ea55110a22910936e`; its size/hash match the build receipt. Extracted engine and custom probe objects are Mach-O ARM64, and the symbol report contains defined `apple_embedded_main`, `apple_embedded_finish` and `PDGodotHostViewController` symbols. SCons completed in 17 minutes 54.60 seconds, with no compiler error; archive warnings concern objects with no symbols. This establishes real native source compilation only. The archive size is not an installed application-size measurement, and the run did not execute a host view, either renderer, lifecycle teardown or a KMP factory.

The subsequent native host test attempt, [run 34423784931](https://github.com/Apdelrahman1911/PartyDeck/actions/runs/34423784931) at PartyDeck commit `c80a5fc1c95ae5a7e26c1bb49e4f885b15d84352`, failed before XCTest execution. The Xcode 26.4.1 ARM64 simulator linker reported `ld: library 'partydeck_godot_ios_probe' not found`; its invocation included the custom library names without their artifact-directory search path. The retained `host-test.log` records the failed host link and cancelled tests. The partial application archive does not establish a linked or executed host; native runtime and lifecycle qualification remain open.

The next [host test run 34425278588](https://github.com/Apdelrahman1911/PartyDeck/actions/runs/34425278588), at commit `c76c540a8245afca315cc16a5c62e39a095d9369`, confirms that the archive search-path correction reached the linker. The host link then failed on four undefined symbols: `_SDL_IsAppleTV`, `_SDL_IsIPad`, `godot_apple_embedded_plugins_initialize()` and `godot_apple_embedded_plugins_deinitialize()`. Xcode again cancelled testing before any XCTest case executed. Full logs, native archives and the partial application artifact are retained; this attempt established neither a linked host nor a native lifecycle pass.

[Host test run 34428221586](https://github.com/Apdelrahman1911/PartyDeck/actions/runs/34428221586), at commit `a04f22be8623bec9fbf38d4e74d8f96a94ebb225`, resolves those symbols and executes all five native XCTest cases: pre-construction cancellation passes, while the four cases that attempt engine startup fail. The retained `.xcresult` app stdout/stderr records the same cause in all four processes: `Main::setup` rejects the host's `--path` argument because the engine was compiled without path-override support, explicitly requiring `disable_path_overrides=no`. The failed scene's exported measurements show one bootstrap attempt, `setup2Succeeded=false`, no Ready events, draws or iterations, and the process quarantined with its OS singleton still present. The host therefore links and launches, but this run does not execute a Godot scene or qualify initialized-engine cleanup, rendering or lifecycle behavior. Its diagnostic screenshot title is not evidence of successful rendering. The downloaded ZIP matches the GitHub artifact digest; app logs, measurements, original captures and the complete result bundle remain retained.

The path-override correction passes in [diagnostic host run 34431377938](https://github.com/Apdelrahman1911/PartyDeck/actions/runs/34431377938), at commit `4f7776aba5809330fdd0197623a03f7a45cba3a1`. All five actual XCTest cases pass with no skips or failures. Nine original PNG/measurement pairs record real `GDTOpenGLLayer` rendering, privacy cover on pause, resumed drawing, touch-generated Exit, deferred cleanup after a draw, immediate foreground loss/resume, cancellation before construction, checked initialization close, and a real missing-main-scene startup failure. Every initialized closed case records one cleanup at depth zero, released view/controller, no OS singleton or render loop, no quarantine, and restoration of the prior true/false idle-timer policy. The full lifecycle case records one bootstrap, one Ready, one Exit and refusal to reopen the same process. Downloaded archive, executable and diagnostic-source hashes match their receipts and exact source revision. This establishes the diagnostic Simulator host and its exercised lifecycle; Last Light 2D/3D gameplay, same-process reinitialization, a KMP factory, and physical-device/Metal qualification remain separate.

Sources:

- [Godot 4.7.2 release](https://github.com/godotengine/godot/releases/tag/4.7.2-stable).
- [Official Android library guide source](https://github.com/godotengine/godot-docs/blob/stable/tutorials/platform/android/android_library.rst). The rendered documentation endpoint returned HTTP 403 in this environment; the official source was read instead.
- [Official iOS export guide source](https://github.com/godotengine/godot-docs/blob/stable/tutorials/export/exporting_for_ios.rst).
- [Official iOS plugin guide source](https://github.com/godotengine/godot-docs/blob/stable/tutorials/platform/ios/ios_plugin.rst).
- [LibGodot API at the researched release](https://github.com/godotengine/godot/blob/4.7.2-stable/core/extension/libgodot.h).
- [Apple application delegate source at the researched release](https://github.com/godotengine/godot/blob/4.7.2-stable/drivers/apple_embedded/godot_app_delegate.h).

## Proposed common boundary

These are PartyDeck-owned concepts, not claims about upstream Godot API names:

1. A `GameCatalog` contains stable game identifiers, player limits, presentation choice, and the metadata required by the shell. The first catalog contains only the implemented Compose game.
2. An optional embedded-game factory advertises its supported game/protocol identifiers. An absent factory means the engine capability is unavailable. There is no fake engine returning successful initialization.
3. The shell creates a presentation session only when entering a supported game and releases it when leaving. Navigation, settings, permission prompts, network ownership, and application lifecycle remain outside the engine.
4. Shell-to-renderer commands carry initialization, a **recipient-safe** game view, lifecycle changes, and disposal. Renderer-to-shell events carry readiness, player intents, completion, and explicit failures. There are no frame/tick, per-card transform, or animation-position messages.
5. The host-authoritative domain validates all player intents regardless of renderer. The engine never receives host-private decks, other players' hands, reconnect secrets, or authority to choose random outcomes.
6. The boundary uses a versioned message envelope and a bounded payload. Protocol/session mismatches, stale messages after disposal, duplicate readiness, and unsupported games fail explicitly. Rendering data does not become a second networking protocol or source of game rules.

A small common interface plus platform factory is enough. Keep platform view types out of `commonMain`; add Android/iOS adapters only when there is a real engine implementation. Wire the game catalog into the first-release UI so that it is an exercised boundary rather than an unused architecture sample. The implemented `EmbeddedGameSession.events` uses the existing coroutines dependency's [`Flow`](https://kotlinlang.org/api/kotlinx.coroutines/kotlinx-coroutines-core/kotlinx.coroutines.flow/-flow/). Its official contract does not imply that a stream is hot or cold, or that initial events are replayed; the PartyDeck interface explicitly requires retaining readiness until subscription and a bounded, ordered stream.

Before a Godot game can ship on either platform, require a real scene and event round trip, entry/exit/re-entry, foreground/background transitions, host-view ownership, teardown without a running render loop, hidden-information checks, device GPU/input/audio checks, and a reproducible engine/assets build. Android and iOS must each pass independently. Optional engine support must never hold the Compose shell open or start in the lobby.

## CI toolchain and action selection

The coordinator selected Kotlin **2.4.20**, Compose Multiplatform **1.12.0**, Gradle **9.7.0**, AGP **9.3.1**, and JDK **21**. The [official Kotlin compatibility table](https://kotlinlang.org/docs/multiplatform/multiplatform-compatibility-guide.html) lists Kotlin 2.4.20 with Gradle through 9.7.0, AGP through 9.3.1, and Xcode 26.4.

Use `ubuntu-24.04` for Android/common validation and **`macos-26`** for Apple validation. GitHub's [hosted runner reference](https://docs.github.com/en/actions/reference/runners/github-hosted-runners) identifies `macos-26` as ARM64; the separate Intel label is `macos-26-intel`. The [official ARM64 manifest](https://github.com/actions/runner-images/blob/main/images/macos/macos-26-arm64-Readme.md) lists **`/Applications/Xcode_26.4.1.app`**. Set `DEVELOPER_DIR=/Applications/Xcode_26.4.1.app/Contents/Developer`, and print `xcodebuild -version`, `xcodebuild -showsdks`, Java, and wrapper versions. Do not silently fall back to the runner's default Xcode 26.6 or a preview. Use `iosArm64` and `iosSimulatorArm64`; no Intel target is needed for this runner.

Official repository release metadata and commit endpoints resolved these action pins:

| Action | Release | Commit |
| --- | --- | --- |
| `actions/checkout` | v7.0.1 | `3d3c42e5aac5ba805825da76410c181273ba90b1` |
| `actions/setup-java` | v6.0.1 | `de7274f081f381c8f8158605e0321c36c376e2e6` |
| `actions/upload-artifact` | v7.0.1 | `043fb46d1a93c77aae656e7c1c64a875d1fc6a0a` |
| `actions/download-artifact` | v8.0.1 | `3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c` |
| `gradle/actions/setup-gradle` | v6.3.0 | `9c971963bec38e04b3d30dcc455b5382be2fdbfb` |
| `android-actions/setup-android` | v4.0.1 | `40fd30fb8d7440372e1316f5d1809ec01dcd3699` |

Pin the full commits with release comments, as [GitHub's security guidance recommends](https://docs.github.com/en/actions/reference/security/secure-use). `android-actions` is a community-maintained project; prefer its small SDK setup task over importing an unrelated build framework. Explicitly request the selected platform/build-tools packages. Repository builds use the checked-in wrapper, never a runner-preinstalled Gradle.

[Gradle's v6.3.0 documentation](https://github.com/gradle/actions/blob/v6.3.0/docs/setup-gradle.md) states that setup automatically validates wrapper JARs. Do not add a redundant wrapper-validation action. Set **`cache-provider: basic`**: this explicitly selects the free open-source cache provider instead of the default proprietary enhanced provider, whose private-repository availability is described as a free preview. Do not also enable a second Gradle cache in setup-java. PR caches should be read-only. Use `contents: read`, checkout without persisted credentials, and normal `pull_request` events. CI has no store credentials.

The Godot comparison jobs use the official [download-artifact v8.0.1 contract](https://github.com/actions/download-artifact/blob/v8.0.1/README.md) to obtain a named debug APK/PCK artifact from the same workflow run. Digest mismatch fails by default; this path requires no additional token. The action uses Node 24 and requires runner 2.327.1 or newer. Artifact transfers do not preserve executable permissions, so a downloaded raw desktop launcher needs its execute bit restored or invocation through Bash. Real desktop rendering adds Ubuntu's [`xauth`](https://packages.ubuntu.com/noble/xauth) and [`libgl1-mesa-dri`](https://packages.ubuntu.com/noble/libgl1-mesa-dri) packages; [`LIBGL_ALWAYS_SOFTWARE=true`](https://docs.mesa3d.org/envvars.html) selects Mesa software GL for the hosted Xvfb run.

The first [Godot comparison run 34427418175](https://github.com/Apdelrahman1911/PartyDeck/actions/runs/34427418175), at commit `c0474960f76459bf07160ec3dba6dcbb084af3ea`, passed its shared build/export job, 18 JUnit cases and 46 Android-checker host cases. Both actual desktop renderers exited successfully with identical 42-view authority traces; all 22 retained screenshot hashes and their provenance matched. A check using that exact commit's renderer source accepted the downloaded PCK, SHA-256 `6bb343ada85e9554ae8886159c6a0461b9232ce5eff864e0ca424f0a7189caa8`. The debug APK, unsigned release APK and release AAB all embed that same pack. Their package sizes are **314,300,296**, **311,433,741** and **104,806,604 bytes**, respectively; these are archive sizes, not installed or per-device split sizes.

All four independent Android cases—2D/3D at normal/200% font scale—passed fresh API 35 AVD preparation without a reboot, verified 720 × 1600 at 280 dpi, and installed the exact downloaded debug APK, SHA-256 `878b0074bef8adef5a68f0741d2931fca74e4e9201d7cbd16894fea44c255f23`. Each entered the native chooser and tapped its actual renderer launch button, then failed during match entry when `adb shell cat /sdcard/partydeck-ci-ui.xml` returned exit 1. No case retained a successful gameplay-readiness assertion or accepted gameplay intent. The entry loop lacked the baseline wait helper's handling for transient UI-dump failures; later diagnostics successfully dumped the UI but overwrote the original dump log. The failing command remains recorded in each result and job log. This run therefore establishes desktop execution and native packaging/launch progress, while Android gameplay and lifecycle qualification remain open pending the corrected checker's execution.

The second [Godot comparison run 34430556814](https://github.com/Apdelrahman1911/PartyDeck/actions/runs/34430556814), at commit `83874c164df7c4a8ee73aad10e7b98b77d67c4d7`, passes 24 JUnit cases, 54 checker host cases, both desktop renderers and all 22 capture-hash checks. Its ordered PCK passes the exact commit's source check, SHA-256 `26bfbb72efa55b63bef2e0ab989c82356c75b3bb3ca40bcf1c5e70e931bff890`; both APKs and the AAB embed that pack. All four native cases install the verified debug APK, SHA-256 `9756e81315b746dc900644197763d677c89c27d5a3e174867e3baa12e651f3f8`, and reach a more specific failure after the fresh-UI handling correction: the actual Godot process reports `PartyDeck Android bridge did not provide its display density.` The renderer's `get_display_scale` method check fails even though the private host measurements record density 1.75. Every case records `readyAccepted=false` and zero accepted intents. The improved failure snapshots and process logs preserve that evidence before and after final diagnostics. The 3D/200% process also reports `Unable to exit the renderer within 1500 ms... Force quitting the process.` This message remains a failed teardown gate despite the host's destroy-returned flag; no native gameplay or lifecycle pass is claimed.

## Required validation jobs

### Android and shared behavior

- Run the shared rule/session/protocol/catalog behavior suites on JVM through their actual registered Gradle tasks.
- Build `:androidApp:assembleDebug` and run `:androidApp:lintDebug` plus meaningful Android-local tests. Build the optimized unsigned APK and AAB with `assembleRelease` and `bundleRelease` at qualification.
- Install exact selected Android SDK/build-tools packages: compile SDK **37.1**, target API **36**, minimum API **26**, and Build Tools **36.0.0**. The resolved Compose/lifecycle AAR metadata required raising the compile SDK after the initial research; see [toolchain evidence](toolchain.md).
- Upload test/lint reports on failure as well as success. Upload the APK and unsigned release bundle under clearly named artifacts. Never label a debug or unsigned artifact as store-ready.
- The implemented emulator smoke exercises debug and a separately test-signed optimized APK on one boot, with distinct evidence directories. It covers rules, settings persistence, hand interaction/privacy, host/invite/leave, invalid join recovery, and large text through actual accessibility nodes. It requires KVM, defaults to a standard API 35 AVD, and keeps ready-screen captures and failure diagnostics. API 36 remains selectable and has an unresolved runtime gate on the current two-vCPU runner. It cannot prove several physical Android/iOS devices interoperate over a local network.

### iOS on macOS

- Run the common behavior suites on `iosSimulatorArm64`, not only compile metadata on Linux. Build the iOS device framework as an additional architecture check at qualification.
- Build the real `iosApp/PartyDeck.xcodeproj`, shared scheme `PartyDeck`, with the Compose framework integrated through `:composeApp:embedAndSignAppleFrameworkForXcode`.
- Follow the [official direct-integration guidance](https://kotlinlang.org/docs/multiplatform/multiplatform-direct-integration.html): declare a framework binary, run the Gradle phase before Compile Sources, and disable User Script Sandboxing for that phase/project configuration. Do not invoke the embedding task outside the required Xcode build environment and claim it proves the app build.
- Discover an available iPhone simulator from the selected Xcode installation rather than hard-code a device marketing name or stale UDID. Run `xcodebuild test` against app-hosted `PartyDeckTests` and `PartyDeckUITests`, with a concrete simulator destination and `CODE_SIGNING_ALLOWED=NO`. The platform owner supplies native transport tests and launch/navigation tests asserting actual shared-UI accessibility markers.
- Preserve `xcodebuild` failure status through any log piping. Save the `.xcresult`, build log, and a simulator screenshot. Tar the `.app` before artifact upload when retaining executable permissions matters; the [artifact action documents archive permission loss](https://github.com/actions/upload-artifact/blob/v7.0.1/README.md#permission-loss).
- Record the exact Xcode, SDK, simulator runtime, and CI run URL in qualification evidence. A framework-only link is not an application launch test; a launch test is not device networking/privacy verification.
- After Debug XCTest passes, build the actual Swift application in Release for `generic/platform=iOS` with `-sdk iphoneos` and signing disabled. Require the optimized `linkReleaseFrameworkIosArm64` task in the build log. A device Kotlin framework alone does not compile Swift code behind device-only compilation conditions.

Apple documents the `xcodebuild test`, `build-for-testing`, `test-without-building`, scheme, and destination commands in [TN2339](https://developer.apple.com/library/archive/technotes/tn2339/_index.html). Additional installed-tool options should be verified from `xcodebuild -help` and `xcrun simctl help` on the runner before use.

Android command sources: [emulator command-line options](https://developer.android.com/studio/run/emulator-commandline), [KVM and `-accel-check`](https://developer.android.com/studio/run/emulator-acceleration), [AVD manager options](https://developer.android.com/tools/avdmanager), and [ADB install/activity/screenshot commands](https://developer.android.com/tools/adb). The current AVD manager page labels that tool deprecated in favor of the new Android CLI, but it remains present in the pinned SDK command-line tools and documents the used `create avd` flags. Installed-tool help verified `pixel_7` as a device profile, the `--device`/`--package`/`--path` options, and `-gpu swiftshader` support. AVD creation here is a small CI setup step, not an application dependency.

Independent review verified Kotlin 2.4.20's published `KotlinNativeSimulatorTest.device` option and found its default runtime selection can select a newer installed runtime than the active Xcode SDK. Both native workflows therefore pass `--device` with the selector's matching simulator UDID to every native test task. The manual Apple toolchain smoke has independently passed 35 tests (14 core, 21 session, no failures/errors/skips) on [run 34370981268](https://github.com/Apdelrahman1911/PartyDeck/actions/runs/34370981268), using Xcode 26.4.1, iOS SDK 26.4, and iOS simulator runtime 26.4.1. This scoped result does not establish full-app qualification.

Set bounded job timeouts and concurrency cancellation for superseded PR runs. Keep the macOS work parallel to the Linux job, since both are required shipping targets. Use small, retained artifacts instead of publishing whole Gradle caches or signing directories.

### Optimized runtime and fixture qualification

The Android runtime copy uses a fresh two-day test certificate generated with JDK 21 `keytool`; no publisher signing variable is accepted. The source and destination must be different resolved files/inodes. Verify that the input is unsigned, align a temporary copy, sign that copy with `--debuggable-apk-permitted false`, verify its signature/alignment, and record the unchanged original SHA-256. The keystore lives in runner temporary storage and is deleted; artifact globs include APKs and public verification records, never the key. Different debug/test certificates require an explicit uninstall between variants. Independent release review executed the helper against the real optimized APK and checked signature/alignment, original ZIP entry bytes, production-variable rejection, and exact/symlink/hard-link input/output rejection.

The [Android apksigner reference](https://developer.android.com/tools/apksigner) documents `--out`, environment password inputs, and signature verification. The [zipalign reference](https://developer.android.com/tools/zipalign) requires alignment before signing and recommends `-P 16` for uncompressed native libraries. [JDK 21 keytool](https://docs.oracle.com/en/java/javase/21/docs/specs/man/keytool.html) documents key generation, validity, and `:env` password modifiers. Installed Build Tools 36.0.0 help independently confirmed the actual signing/alignment options, including disabling debuggable signing and preserving alignment.

Run 34378549932 passed 104 Android/JVM tests and lint/package tasks, but its accelerated emulator smoke stopped at the first Home assertion because a **System UI ANR dialog** covered the rendered application. It is not runtime acceptance. The retained launch output reports `Status: ok`; the screenshot/XML identify the system dialog. The next revision releases Gradle build workers before the emulator, records host memory/CPU use, allows up to four available CPU cores, and uses 3 GiB RAM. Launcher readiness and explicit crash-dialog checks must pass. The emulator's own log reported an effective 2560 MiB minimum and recommended four cores for the previous configuration. Installed `-help-dpi-device` reports that option obsolete/ignored, so it is not used to claim a display change.

The Android retry of run 34384326819 failed the strict launcher check before PartyDeck was installed: `SystemUIService` took 20,064 ms to execute. Preserved full logcat reports guest CPU pressure `avg10=88.93`, total CPU 99–100%, and memory full pressure zero; the host had 6,615 MiB available before startup and no Gradle/Kotlin process in its process snapshot. The evidence supports first-boot CPU saturation, not another RAM increase. The optimized variant subsequently passed real rules/settings/practice/host/invitation/ordinary Join flows on that boot, then failed text-edit verification at 200% text. Neither variant passed acceptance, and a transport test also failed in the build phase despite continued package production.

Preparation now runs separately while PartyDeck is absent. It uses the same strict launcher checks and preserves an initial System UI ANR's XML, logcat, events and result before allowing **one** clean ADB reboot. Only that observed failure permits recovery; missing evidence, other errors, failed settings restoration, an installed PartyDeck package, or any failure after the reboot stops preparation. A changed kernel boot ID and completed boot are required before the second launcher check. Both APK acceptance runs then execute once on the prepared boot, with their original crash/ANR rejection intact. No crash dialog is dismissed. Android's [ANR diagnosis guide](https://developer.android.com/topic/performance/anrs/diagnose-and-fix-anrs) documents the 20-second foreground-service execution deadline and system-load analysis. The authoritative [ADB manual](https://android.googlesource.com/platform/packages/modules/adb/+/refs/heads/main/docs/user/adb.1.md) documents `reboot`; the [Linux kernel random sysctl reference](https://docs.kernel.org/admin-guide/sysctl/kernel.html#random) documents the per-boot identifier. The helper reuses the existing CLI's checks through Python's [documented source-file import recipe](https://docs.python.org/3/library/importlib.html#importing-a-source-file-directly).

The runtime step has a 30-minute bound inside the existing 45-minute Android job: the observed build took about eleven minutes, and one partial optimized UI run took about six and a half minutes before the added focus/clear checks and final large-text practice flow. Individual boot, UI and ADB deadlines remain bounded. Script duration on this hosted software-rendered emulator is not a product performance measurement.

Run 34398824935 passed all **117 Android/JVM tests**, lint and package tasks. Its isolated empty-AVD preparation correctly failed on System UI ANRs before and after the single reboot, preserving both attempts and distinct boot IDs; neither APK was installed. On the second boot, RenderEngine reported compiling 134 shaders in **43,864.5 ms**, with CPU pressure `avg10=79.35` and SurfaceFlinger/RenderEngine dominating the trace. Changing display pixels alone would not establish a fix for that compilation cost. The next bounded candidate uses **`-gpu swangle`**, retaining the same Pixel 7 display, API, KVM, memory, CPU and strict ANR policy. The [official acceleration reference](https://developer.android.com/studio/run/emulator-acceleration) and installed emulator 37.1.11.0 `-help-gpu` distinguish `swiftshader` (SwiftShader GLES/Vulkan) from `swangle` (ANGLE with SwiftShader for GLES, SwiftShader for Vulkan). This tests the supported alternate GLES implementation; deprecated `*_indirect` flags are not used. The new run must supply execution evidence before the candidate is accepted.

Manual Validate runs now accept a choice input `platform=all|android|ios`, defaulting to both; normal push/pull-request events still select both jobs. GitHub documents [choice inputs and their string values](https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax#onworkflow_dispatchinputs), [manual dispatch against a branch/tag and its resulting commit](https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows#workflow_dispatch), and that [commit skip instructions affect only push/pull-request events](https://docs.github.com/en/actions/how-tos/manage-workflow-runs/skip-workflow-runs). This permits an explicitly coordinated Android-only qualification after a CI-only fix without repeating unchanged iOS compilation. Verify the dispatched run's `headSha`, and cite the earlier iOS revision explicitly instead of claiming that a skipped job executed.

Android-only run 34402895350 confirmed the ANGLE backend and reduced shader priming to **10.80 s / 3.57 s** on the first boot/reboot, from **49.60 s / 43.86 s**. Both preparation attempts still hit System UI service ANRs; second-boot CPU was 100%, CPU pressure `avg10=84.38`, and the graphics composer used 25%, with zero full memory pressure. The next candidate retains `swangle` and reduces the framebuffer from 1080 × 2400 at 420 dpi to **720 × 1600 at 280 dpi**: 44.4% as many pixels, with the same approximately 411 × 914 dp viewport. Before boot, the wrapper sets `hw.lcd.width`, `hw.lcd.height`, `hw.lcd.density`, `skin.name`, and `skin.path`; after strict preparation it requires fresh physical/override `wm` values to match before either APK can install. Expected/actual values and the applied configuration are preserved. AOSP's [AVD loader](https://android.googlesource.com/platform/external/qemu/+/refs/heads/emu-master-dev/android/emu/avd/src/android/avd/info.c) confirms config.ini overrides skin hardware settings, numeric WIDTHxHEIGHT skins, and skin.path precedence; its [hardware configuration loader](https://android.googlesource.com/platform/external/qemu/+/refs/heads/emu-master-dev/android/emu/avd/src/android/avd/hw-config.c) loads integer density values and handles 280 dpi. Installed emulator 37.1.11 defines the same LCD keys. Deadlines, the single preparation recovery, shader priming, and app assertions are unchanged; acceptance still requires a new executed run.

Run 34406555185 confirmed 720 × 1600 at 280 dpi in both preparation attempts, but System UI still exceeded its service deadline before either APK was installed. The functional baseline now selects the official **standard API 35 x86_64 revision 2** image, with API 36 explicitly selectable through `PARTYDECK_ANDROID_API` or the manual `android_api` input. Both entry points reject other values before image/AVD work. The image's `source.properties` and each preparation attempt's actual guest API are retained; actual API equality is required before APK acceptance. Google's [standard-image repository metadata](https://dl.google.com/android/repository/sys-img/android/sys-img2-4.xml) publishes both standard images. Android 15's [handheld system components](https://android.googlesource.com/platform/build/+/refs/tags/android-15.0.0_r1/target/product/handheld_system_ext.mk), [handheld product components](https://android.googlesource.com/platform/build/+/refs/tags/android-15.0.0_r1/target/product/handheld_product.mk), and [base system](https://android.googlesource.com/platform/build/+/refs/tags/android-15.0.0_r1/target/product/base_system.mk) retain Launcher3, SystemUI, LatinIME and IntentResolver for the same interactive checks. The app still targets API 36 with minimum API 26. **API 35 qualification does not close the unresolved API 36 runtime gate**; no ANR timeout, shader behavior, recovery limit or application assertion was relaxed.

The first API 35 run, 34409391037, booted in 40.23 seconds with no retained ANR/fatal event, but failed preparation because HOME resolution cached `com.android.sdksetup/.DefaultActivity` before the setup handoff. Fresh UI evidence showed responsive `com.android.launcher3` instead. Neither APK was installed. The readiness fix now resolves HOME for every fresh UI tree after the Home key, bounded by the existing 90-second readiness deadline. Owner and independent replays cover both setup-handoff timings, unrelated-screen rejection, insufficient matching trees, resolver timeout and immediate crash/ANR rejection. Three fresh matching launcher checks and all recovery/application assertions remain in place.

[Run 34413839964](https://github.com/Apdelrahman1911/PartyDeck/actions/runs/34413839964), at commit `09f35a5891f3912e22b4f8ced7907fe9e945b416`, proves the HOME fix on the real API 35 emulator: preparation passed on its first boot without a reboot, and the actual guest API and 720 × 1600 / 280 dpi display matched the required configuration. All 117 JVM/local tests, lint and packaging passed; reports and debug, unsigned release, test-signed release and AAB packages were downloaded. The two executed APK hashes match their downloaded packages. Both APKs passed standard-size rules, settings restart, hand privacy/background/play/leave, hosting/invitation/share teardown and invalid-invitation recovery. Both also captured a visible soft keyboard at standard and 200% font scale. Each then failed the same large-text Join assertion, `Expected focused editable join-invitation within 45 seconds`. The final app XML omits that field below the reduced scroll viewport while the software keyboard is visible; the focused-field wait did not request scrolling after the keyboard resize. Full debug and optimized APK runtime acceptance therefore remains open. No retained ANR or fatal exception was found in either variant's logcat.

The focused-field wait now uses the existing bounded viewport scrolling after the keyboard resize. [Run 34417204089](https://github.com/Apdelrahman1911/PartyDeck/actions/runs/34417204089), at commit `ed440c8cece8e83f39ce75706e9827e9ba111b7b`, passed all 117 tests, lint and packaging and again verified first-boot API 35/display preparation. The **optimized test-signed APK passed every runtime flow**, including large-text Join editing/recovery, private-hand interaction and play-action reachability, with 26 stage captures and no diagnostic or environment-restoration errors. Debug advanced past the previous focus assertion, but its intended Join-submit tap coincides with a logged system Home-button activation and PartyDeck pause; a subsequent launcher swipe opened the notification shade. The validation-feedback wait then timed out. Its final XML and screenshot show that shade, so this does not establish that app validation feedback was absent. The exact debug tap geometry was not retained; an optimized capture shows that the Join-submit semantics can extend below the app scroll viewport into the navigation-bar region, which the original visibility check did not exclude because it only used physical display bounds. No ANR or fatal exception was found in either retained logcat, and both executed APK hashes match the downloaded packages. The combined gate failed in this run because debug did not complete. The debug notification-shade screenshot is infrastructure evidence and is excluded from the app screenshot gallery.

[Run 34421746656](https://github.com/Apdelrahman1911/PartyDeck/actions/runs/34421746656), at commit `d3418f150e908667560767e4b825f8968b337df3`, passed the existing **API 35 debug and optimized APK smoke suite** after the independently reviewed viewport-clipping fix. All 117 tests, lint and packaging passed; both APKs completed its scripted standard and 200% text flows with 26 stage captures each, visible soft-keyboard evidence, and no diagnostic or settings-restoration errors. Both executed APK hashes match the downloaded packages. The retained Launcher3 ANR at 00:45:38–39 UTC occurred during empty-AVD preparation, before PartyDeck installation; Android classified it as expired with an ANR dump only. Three matching launcher checks passed at 00:45:54, and debug preparation passed again at 00:46:06 before installation. No reboot or crash-dialog dismissal occurred. This pre-install infrastructure event remains in the evidence; it is not an app runtime failure or an ANR-free boot claim. API 36, physical-device LAN/GPU/accessibility and publisher-signing gates remain separate.

Later independent visual review found a coverage gap in that suite: the optimized Rules practice label was visible for only 5 pixels at normal text and 26 pixels at 200% text, while its clickable parent extended below the scroll viewport at y=1516. The checker accepted the already-clipped text bounds and returned with Back; neither variant activated the Rules practice button. That route therefore was not qualified by this run. The follow-up requires the complete reported clickable parent, verifies Rules-specific content after navigation because Home has the same practice label, actually taps the Rules action, and requires gameplay followed by confirmed leave and Home. Nine host regressions and independent retained-tree/route-failure replays pass. The controller also has a separately reviewed fix for a new practice session previously starting behind the Rules overlay. An optimized post-play capture similarly truncates the Next round label to 16 pixels; the original image retains that visual limit without invalidating unrelated executed flows.

[Baseline run 34428798269](https://github.com/Apdelrahman1911/PartyDeck/actions/runs/34428798269), at commit `15ab6408d16c04941d133214b03d4e8c6c42ced9`, executes that Rules fix. All **118 Android/JVM tests**, lint and packages pass; debug and optimized test-signed APKs complete their scripts with 30 captures each, matching downloaded/executed APK hashes, and no diagnostic or environment-restoration errors. Fresh API 35/display preparation passes without a reboot; the retained preparation and variant logs contain no matched ANR/fatal event. Original input records and XML prove all four Rules-to-game-to-confirmed-leave-to-Home routes at normal and 200% text. All four practice labels are readable. However, the optimized normal capture still clips about 10 pixels from the button's rounded lower border: Compose reports the parent itself clipped to `[42,1428][678,1516]`, exactly touching the scroll boundary. The other three show the complete button. Functional route acceptance therefore does not establish complete visual containment in all four captures; the original image and this residual limit remain recorded.

The same baseline revision passes **83 shared iOS Simulator tests** and **six app XCTest cases**: three native transport tests and three UI routes covering settings, hosting/invitation and playable practice. The actual Java–Swift exchange passes in both directions, with successful Java and Xcode exits and both remote-close results. Eight original native app captures are retained. Its optimized unsigned device application also builds successfully: the log executes `:composeApp:linkReleaseFrameworkIosArm64`, and the downloaded app contains an ARM64 executable with iPhoneOS 26.4 metadata. The archive is **15,624,845 bytes**, SHA-256 `d77f59a8272803879a31ed6b56fb32db7d1ab4ae8deae940f7874e871e10b89f`; this is a compressed archive size. The command disables signing, and the retained app contains no signature directory or provisioning profile. Simulator execution and unsigned device compilation do not establish physical-device or publisher-signing qualification.

[Android recapture 34432935952](https://github.com/Apdelrahman1911/PartyDeck/actions/runs/34432935952), at commit `8d8d429186bfa3fc895b943101a24c7aeb992163`, passes the follow-up viewport-clearance check. All 118 tests, lint and packages pass; both debug and optimized test-signed APKs complete their 30-capture scripts with matching downloaded/executed hashes and no diagnostic or environment-restoration errors. Direct inspection of all four original Rules action captures confirms readable labels and complete rounded button borders: each ends at y=1453, leaving 63 pixels before the viewport bottom at y=1516. Original XML and input records again prove all four Rules-to-game-to-confirmed-leave-to-Home routes. This resolves the earlier button-border capture limit for the executed API 35 normal/200% text cases; the historical originals remain retained.

The first Android attempt of run 34384326819 stopped before Gradle because the runner's unrelated Google Chrome APT feed returned a Packages checksum mismatch. The prior run proves only `libegl1` and its `libegl-mesa0` dependency needed installation; the other six requested graphics packages were already installed. Setup now checks installed package status first. If packages are missing, APT reads only the runner's existing `/etc/apt/sources.list.d/ubuntu.sources`, with an empty source-parts directory and three bounded acquisition retries. It preserves the file's signed-keyring configuration and normal package integrity checks. GitHub's [exact image source setup](https://github.com/actions/runner-images/blob/ubuntu24/20260907.300/images/ubuntu/scripts/build/configure-apt-sources.sh) confirms that file and its Ubuntu mirror list. The [Ubuntu APT configuration](https://manpages.ubuntu.com/manpages/noble/en/man5/apt.conf.5.html), [source format/signing reference](https://manpages.ubuntu.com/manpages/noble/en/man5/sources.list.5.html), and [dpkg-query format reference](https://manpages.ubuntu.com/manpages/noble/en/man1/dpkg-query.1.html) document the selected behavior; installed `apt-config` confirms the source-list/source-parts keys.

The Java–Swift TLS fixture contract, test-only classpath, manifest, deadlines, and required result are recorded in [interop research](jvm-swift-interop.md). Xcode's `TEST_RUNNER_<VAR>` prefix stripping is documented in the [verbatim xcodebuild man-page mirror](https://keith.github.io/xcode-man-pages/xcodebuild.1.html); Apple's old archived man URL returned 404. CI records its installed `man xcodebuild`/help as evidence. Actual delivery is enforced by the named native XCTest passing plus a successful JVM exit and complete two-direction byte-count/remote-close result. Missing forwarding cannot become a silent skip. The supervisor polls output and the JVM process, has a 30-minute Xcode deadline, and terminates only its owned process groups with bounded cleanup, using the documented [Python subprocess](https://docs.python.org/3/library/subprocess.html) and [os.killpg](https://docs.python.org/3/library/os.html#os.killpg) behavior.

[Apple's build settings reference](https://developer.apple.com/documentation/xcode/build-settings-reference) documents `LD_GENERATE_MAP_FILE` and the default per-target/architecture map path. Both Xcode commands enable maps; CI preserves them for final symbol/license review. Kotlin's verified Release mapping and Swift optimization settings are recorded by the iOS owner; a simulator or unsigned optimized device app remains separate from signed distribution and physical-network qualification.

The next workflow separates shared-native validation, Debug simulator/XCTest, and optimized device compilation. Shared reports are uploaded before Xcode begins; simulator evidence is uploaded before the Release device step. Each Xcode step has a 40-minute limit within a 90-minute iOS job, with the test supervisor enforcing its shorter 30-minute command deadline. This preserves completed evidence if a later optimized build exceeds its budget and makes the active phase visible independently of combined script logs.

Run 34384326819 subsequently completed all three native transport XCTest cases (including the real Java–Swift exchange), both UI cases, and the optimized unsigned device build. The Release Kotlin framework task appears in the job log. Artifact creation then failed with `ENOTFOUND`, before any artifact bytes were sent; the job log is preserved, but that attempt produced no downloadable iOS bundle. A small local composite now retries the same pinned uploader once after ten seconds and explicitly fails if both attempts fail. The [pinned action inputs](https://github.com/actions/upload-artifact/blob/043fb46d1a93c77aae656e7c1c64a875d1fc6a0a/action.yml) expose no retry setting and document `overwrite`; its [implementation](https://github.com/actions/upload-artifact/blob/043fb46d1a93c77aae656e7c1c64a875d1fc6a0a/src/upload/upload-artifact.ts) deletes a matching artifact before an overwrite upload. Retry uses that option to handle a partially created artifact. Native build/test failure signals remain independent, and exhausted upload retries remain a workflow failure.

## Release signing contract

Unsigned/debug CI must work with no credentials. Signing is a distinct environment-gated workflow or documented local command, never a silent fallback to a debug key.

- **Android:** the implemented signing configuration consumes `PARTYDECK_KEYSTORE_PATH`, `PARTYDECK_KEYSTORE_PASSWORD`, `PARTYDECK_KEY_ALIAS`, and `PARTYDECK_KEY_PASSWORD` together. All absent produces unsigned release packages; a partial set fails configuration. Distribution CI may decode a Base64 publisher keystore into a runner-temporary path; do not commit it, generate an upload identity during routine validation, or print passwords. The separately named runtime test copy above never consumes these credentials. [Android's signing guide](https://developer.android.com/studio/publish/app-signing) documents keeping signing information out of shared build files.
- **iOS:** release signing requires the selected bundle identifier and Apple team, a distribution certificate/private key (`.p12`), its password, a matching provisioning profile, and export configuration. [GitHub's official certificate guide](https://docs.github.com/en/actions/use-cases-and-examples/deploying/installing-an-apple-certificate-on-macos-runners-for-xcode-development) documents `BUILD_CERTIFICATE_BASE64`, `P12_PASSWORD`, `BUILD_PROVISION_PROFILE_BASE64`, and `KEYCHAIN_PASSWORD` as example secret names. Use a temporary keychain and remove it/profile in an always-running cleanup step. A simulator `.app` cannot be submitted as an iOS release.
- **Distribution:** no App Store Connect or Play upload is implied by a passing build. Store listing, privacy declarations, cross-device local-network testing, accessibility/device performance, and the actual signed archive remain explicit qualification work owned with release review.

The implemented [Validate workflow](../../.github/workflows/validate.yml) and [validation scripts](../../scripts/README.md) use the bootstrapped module task names. A workflow definition is not passing evidence: executed CI results and outstanding device/store checks belong in the qualification record.
