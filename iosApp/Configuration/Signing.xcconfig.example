// Copy to Signing.xcconfig for a local device build. That file is ignored by Git.
DEVELOPMENT_TEAM = YOUR_APPLE_TEAM_ID
// Override only if this bundle identifier is unavailable to your team.
// PRODUCT_BUNDLE_IDENTIFIER = your.registered.partydeck.identifier
